export interface DocumentData {
  // expires: boolean;
  // expiryDate: string | null;
  documentData: string;
  modelType: string;
  certificateYear: string;
  certificateCode: number;
  id?: string;
  // createdAt: string;
  // updatedAt: string;
}
