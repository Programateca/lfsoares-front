export interface DocumentData {
  // expires: boolean;
  // expiryDate: string | null;
  documentData: string;
  modelType: string;
  id?: string;
  treinamento: string;
  year: string;
  // createdAt: string;
  // updatedAt: string;
}
